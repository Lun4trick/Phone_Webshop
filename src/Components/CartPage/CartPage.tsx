import React from 'react';
import PathHistory from '../PathHistory/PathHistory';

const CartPage: React.FC = () => (
  <section>
    <PathHistory />
    <h1 className='font-[32px] text-Phone-white'>
      Cart
    </h1>
  </section>
);

export default CartPage;
